// 1. Number Type
let num1 = 25;
let num2 = 1.5;

let inf = Infinity;
let mInt = -Infinity;

// 수치연산이 실패했을 경우 연산 결과
let nan = NaN;

// 2. String Type
let myNamae = '감자';
let myLocation = '한국';
let intruduce = myName + myLocation;

// backtick: 변수의 값을 동적으로 넣을 수 있다
let intdroduce = `${myName}는 ${myLocation}에 있다`;

// -> 템플릿 리터럴 문법

// 3. Boolean Type
let isSwitchOn = true;
let isEmpty = false;

// 4. Null Type
let empty = null;

// 5. Undefined Type
let none;


// Null은 직접 변수에 할당한 상태
// Undefined은 변수를 미처 초기화하지 못했거나 존재하지 않는 상태