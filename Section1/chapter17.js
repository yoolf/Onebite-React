// 1. 배열 생성
let arrA = new Array();  // 배열 생성자
let arrB = [];   // 배열 리터럴 (대부분 사용)

// 배열 내부에 어떤 타입을 넣어도 된다.
let arrC = [1, 2, 3, 
    true, "hello", null, undefined, () => {}, {}, [],
];
console.log(arrC);
